# npub.cash v2 errors

If npub.cash can not complete an operation it will return respective status codes, as well as a json error response with the following structure

```json
{ "error": true, "message": "This is the error message" }
```
