{
  "name": "@npubcash/api-contract",
  "version": "0.0.0",
  "private": true,
  "description": "Internal shared HTTP contract for npub.cash",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
